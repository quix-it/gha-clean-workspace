# Workspace cleanup

Action used for cleaning a given directory when invoked 

## Outputs

The action produces no outputs.

## Usage

Using defaults (purges current folder when invoked and at the end of the job):
```yaml
...
    - uses: quix-it/gha-clean-workspace@v2
...
```
